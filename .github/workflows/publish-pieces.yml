# Mint evo-kiosk session layer into foonerd/evo-device-audio-artefacts.
name: publish-pieces

"on":
  workflow_dispatch:
    inputs:
      scope:
        description: "What to mint"
        type: choice
        options: [all, evo-kiosk]
        default: all

concurrency:
  group: publish-pieces
  cancel-in-progress: false

env:
  ARTEFACTS_REPO: foonerd/evo-device-audio-artefacts
  COMMIT_USER: evo-kiosk CI
  COMMIT_EMAIL: ci@evoframework.org

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Stage kiosk layer
        env:
          PLUGIN_SIGNING_KEY_PEM: ${{ secrets.PLUGIN_SIGNING_KEY_PEM }}
        run: |
          set -euo pipefail
          if [ -z "${PLUGIN_SIGNING_KEY_PEM:-}" ]; then
            echo "::error::PLUGIN_SIGNING_KEY_PEM is empty — set the audio commons key on this repo (same secret as foonerd/evo-device-audio)"
            exit 1
          fi
          VERSION=$(tr -d '[:space:]' < VERSION)
          SRC=/tmp/kiosk-src
          mkdir -p "$SRC"
          for sub in scripts layer crates; do
            [ -d "$sub" ] && cp -a "$sub" "$SRC/"
          done
          for f in Cargo.toml Cargo.lock README.md DEVELOPING.md Cross.toml VERSION; do
            [ -f "$f" ] && cp -a "$f" "$SRC/"
          done
          umask 077
          printenv PLUGIN_SIGNING_KEY_PEM > /tmp/key.pem
          export EVO_PLUGIN_SIGNING_KEY=/tmp/key.pem
          OUT=/tmp/stage/bundles/evo-kiosk/${VERSION}
          mkdir -p "$OUT"
          bash scripts/release/pack-signed-tree.sh \
            --piece evo-kiosk --version "$VERSION" --kind kiosk-session \
            --src "$SRC" --out-dir "$OUT"
          shred -u /tmp/key.pem

      - uses: actions/upload-artifact@v7
        with:
          name: kiosk-tree
          path: /tmp/stage/bundles/
          if-no-files-found: error
          retention-days: 7

  publish:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: kiosk-tree
          path: /tmp/stage/bundles/

      - name: Push append-only slot
        env:
          GH_TOKEN: ${{ secrets.ARTEFACTS_PUSH_TOKEN }}
        run: |
          set -euo pipefail
          git config --global user.name "${COMMIT_USER}"
          git config --global user.email "${COMMIT_EMAIL}"
          ART=/tmp/artefacts
          git clone "https://x-access-token:${GH_TOKEN}@github.com/${ARTEFACTS_REPO}.git" "$ART"
          cd "$ART"
          NEW=()
          while IFS= read -r -d '' src; do
            rel="${src#/tmp/stage/}"
            dest="$ART/$rel"
            if [ -e "$dest" ]; then
              echo "::warning::Skipping $rel — version slot already published"
              continue
            fi
            mkdir -p "$(dirname "$dest")"
            cp -a "$src" "$dest"
            NEW+=("$rel")
          done < <(find /tmp/stage/bundles -type f -print0)
          if [ ${#NEW[@]} -eq 0 ]; then
            echo "No new pieces to publish."
            exit 0
          fi
          git add bundles/
          git commit -m "publish: evo-kiosk piece"
          git push origin main
